DEGISIKLIK RAPORU
Tarih: 21 Eylul 2026
Dosya: src/decoding.py
Fonksiyon: generate_function_call()


1. NE DEGISTI
-------------

Sadece TEK bir yer degisti: fonksiyon adi uretilmeden once modele
verilen prompt.

ESKI HALI (satir 160-161):

    name_candidate = ['"' + fn.name + '"' for fn in functions]
    input_ids = model.encode(prompt + "\nfunction name: ")[0].tolist()

YENI HALI (satir 160-165):

    name_candidate = ['"' + fn.name + '"' for fn in functions]
    catalog = "Available functions:\n"
    for fn in functions:
        catalog += f"- {fn.name}: {fn.description}\n"
    name_prompt = catalog + "\n" + prompt + "\nfunction name: "
    input_ids = model.encode(name_prompt)[0].tolist()

Eklenen satir sayisi: 4
Silinen satir sayisi: 0 (bir satir degistirildi)

BASKA HICBIR YERE DOKUNULMADI. Ozellikle su bug'lar HALA DURUYOR ve
bilerek duzeltilmedi:
  - Bug 2: is_complete_number tam sayilari kabul etmiyor (grammar.py)
  - ORDINAL_WORDS listesinin 6 eleman ile sinirli olmasi
  - src/vocab.py:19 mypy hatasi
  - flake8 .venv dizinini taramasi


2. NEDEN DEGISTI
----------------

Calistirma sonucunda 11 promptun sadece 4 tanesi dogru fonksiyonu
secmisti. Ozellikle su iki hata goze carpti:

  Prompt 9:  "Replace all numbers in ... with NUMBERS"
             -> fn_get_square_root secildi (yanlis)

  Prompt 10: "Replace all vowels in ... with asterisks"
             -> fn_get_square_root secildi (yanlis)

  Prompt 7:  "What is the square root of 16?"
             -> fn_substitute_string_with_regex secildi (yanlis)

Sebep: modele verilen prompt su sekildeydi:

    Replace all vowels in 'Programming is fun' with asterisks
    function name:

Model bu noktada 5 fonksiyon isminden birini secmek zorundaydi ama
bu isimlerin NE ISE YARADIGINI bilmiyordu. Sadece isme bakip tahmin
ediyordu.

Halbuki functions_definition.json icindeki her fonksiyonun bir
"description" alani var. Bu alan models.py icindeki
FunctionDefinition sinifinda okunuyor ve bellekte duruyor, ama
hicbir yerde kullanilmiyordu.


3. DEGISIKLIK NE YAPIYOR
------------------------

Artik modele su prompt gonderiliyor:

    Available functions:
    - fn_add_numbers: Add two numbers together and return their sum.
    - fn_greet: Generate a greeting message for a person by name.
    - fn_reverse_string: Reverse a string and return the reversed result.
    - fn_get_square_root: <tanim dosyasindaki aciklama>
    - fn_substitute_string_with_regex: <tanim dosyasindaki aciklama>

    Replace all vowels in 'Programming is fun' with asterisks
    function name:

Model artik her fonksiyonun ne yaptigini goruyor ve secimini buna
gore yapabiliyor.

Katalog, tanim dosyasindan calisma aninda uretiliyor. Hicbir
fonksiyon adi veya aciklamasi koda gomulu degil. Tanim dosyasi
degisirse katalog da otomatik degisir. Bu onemli, cunku subject
degerlendirmede farkli tanim dosyalari kullanilabilecegini
soyluyor.


4. CONSTRAINED DECODING BOZULDU MU
-----------------------------------

Hayir. Degisen tek sey modele verilen baglam.

name_candidate listesi aynen duruyor ve generate_constrained_string
hala sadece bu listedeki isimlere goturen token'lari kabul ediyor.
Yani model yine sadece gercekten var olan bir fonksiyon adi
uretebilir.

Subject "fonksiyon LLM tarafindan secilmeli, heuristik ile degil"
diyor. Bu kural da bozulmadi: secimi yapan hala model. Kodda
"eger prompt 'replace' iceriyorsa regex fonksiyonunu sec" gibi bir
kural YOK. Sadece modele karar verebilmesi icin gereken bilgi
veriliyor.


5. SIRADAKI ADIM
----------------

  make run

Beklenen iyilesme: prompt 7, 8, 9, 10 dogru fonksiyonu secmeli.

Bu degisiklik parametre degerlerini ETKILEMEZ. Prompt 2'deki
"a: 2.0, b: 3.0" hatasi (265 ve 345 olmaliydi) devam edecek,
cunku o ayri bir bug (Bug 2 - is_complete_number).
